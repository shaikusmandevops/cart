@Library('roboshop') _

pipeline {
    agent any
    stages {
        stage('code') {
            steps {
                echo 'Hello World'
                script {
                          log.info 'Starting'
                          log.warning 'Nothing to do!'
                        }
            }
        }
    }
}